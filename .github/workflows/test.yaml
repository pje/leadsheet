name: Deno

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v1.1.3
        with:
          deno-version: v1.x
      - name: build
        run: make build
      - name: fmt
        run: make fmt-check
      - name: lint
        run: make lint
      - name: test
        run: make test
